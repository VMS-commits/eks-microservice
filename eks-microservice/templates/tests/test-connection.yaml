apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eks-microservice.fullname" . }}-test-connection"
  labels:
    {{- include "eks-microservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eks-microservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
